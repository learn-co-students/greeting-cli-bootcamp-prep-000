#!/usr/bin/env ruby
require_relative "../lib/greeting.rb"

# code your CLI here!
#outputs a welcome message and asks the user for input
puts "Hi! I'm HAL, what's your name?"
#uses #gets.strip to capture the user input and set it equal to a variable called name
name = gets.strip
#calls on the greeting method with an argument of the user's name
greeting(name)
# puts "Hello #{name.strip}. It's nice to meet you."