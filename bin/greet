#!/usr/bin/env ruby
require_relative "../lib/greeting.rb"

puts "Hi! I'm HAL, what's your name?"
name = gets.strip
greeting(name)

# Note: this still passes the tests when I write gets instead of gets.strip